# Design & Roadmap

**Status: no mod concept chosen yet.** This file gets filled in once one
exists — mirroring civ7-netaji-mod's DESIGN.md, which tracked the
Bose/Bharat concept from idea through each build stage.

## Template for once a concept exists

- **Concept:** one-paragraph pitch.
- **Non-negotiable framing rule:** copy into CLAUDE.md's Conventions section
  too, so it's enforced every session (civ7-netaji-mod's equivalent: no
  Axis-alliance references, ever).
- **Scope:** what ships in v1 vs. deferred.
- **Build stages:** numbered, each with a "done + verified in-game" bar —
  same pattern as civ7-netaji-mod's Stage 1–4 tracking.
- **Open questions:** anything blocked on a hands-on Mod Editor session.
